### Написать ручки на Flask для модуля Auth.

#### Auth:
* POST /login Login user
* POST /refresh Update refresh and access token
* POST /logout Logout user
* GET /test-token Check access_token

Оценка: хз